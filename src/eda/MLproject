name: eda
conda_env: conda.yml

entry_points:
  main:
    command : jupyter notebook --NotebookApp.use_redirect_file=False
    
    # command: jupyter notebook
    # for ubuntu use either (https://github.com/jupyter/jupyter_core/issues/191): 
    # jupyter lab --NotebookApp.use_redirect_file=False 
    # or : jupyter notebook --NotebookApp.use_redirect_file=False
